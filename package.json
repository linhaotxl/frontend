{
  "name": "frontend",
  "private": true,
  "scripts": {
    "init": "npx lerna bootstrap",
    "watch:notes": "npx lerna run --parallel --scope notes watch"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
